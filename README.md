# nvim-config-frontend

## What'd shoud u do?

```
git clone https://github.com/Kiasma1/nvim-config-frontend.git ~/.vim
ln -s ~/.vim/init.vim ~/.config/nvim/init.vim
nvim
```

Using `:PlugInstall` to install all plugins
